<?xml version="1.0" encoding="UTF-8" ?>

<!-- https://www.playframework.com/documentation/latest/SettingsLogger -->

<!DOCTYPE configuration>

<configuration>
  <import class="ch.qos.logback.classic.encoder.PatternLayoutEncoder"/>
  <import class="ch.qos.logback.classic.AsyncAppender"/>
  <import class="ch.qos.logback.core.FileAppender"/>
  <import class="ch.qos.logback.core.ConsoleAppender"/>
  <import class="ch.qos.logback.core.rolling.RollingFileAppender"/>
  <import class="ch.qos.logback.core.rolling.SizeBasedTriggeringPolicy"/>

  <!-- サイズベースログローテーション設定付きファイルアペンダー -->
  <appender name="ROLLING_FILE" class="RollingFileAppender">
    <file>${application.home:-.}/logs/studio-backend.log</file>
    <encoder class="PatternLayoutEncoder">
      <charset>UTF-8</charset>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %cyan(%logger{36}) %magenta(%X{pekkoSource}) %msg%n</pattern>
    </encoder>

    <!-- サイズベースのローテーション -->
    <rollingPolicy class="ch.qos.logback.core.rolling.FixedWindowRollingPolicy">
      <!-- ローテーション後のファイル名パターン -->
      <fileNamePattern>${application.home:-.}/logs/studio-backend.%i.log</fileNamePattern>
      <!-- 最小インデックス -->
      <minIndex>1</minIndex>
      <!-- 最大インデックス（10ファイルまで保持） -->
      <maxIndex>10</maxIndex>
    </rollingPolicy>

    <!-- サイズベースのトリガー -->
    <triggeringPolicy class="SizeBasedTriggeringPolicy">
      <!-- ファイルサイズが5MBを超えたらローテーション -->
      <maxFileSize>5MB</maxFileSize>
    </triggeringPolicy>
  </appender>

  <!-- 従来のファイルアペンダー（バックアップ用） -->
  <appender name="FILE" class="FileAppender">
    <file>${application.home:-.}/logs/studio-backend.log</file>
    <encoder class="PatternLayoutEncoder">
      <charset>UTF-8</charset>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %cyan(%logger{36}) %magenta(%X{pekkoSource}) %msg%n</pattern>
    </encoder>
  </appender>

  <appender name="STDOUT" class="ConsoleAppender">
    <!--
         On Windows, enabling Jansi is recommended to benefit from color code interpretation on DOS command prompts,
         which otherwise risk being sent ANSI escape sequences that they cannot interpret.
         See https://logback.qos.ch/manual/layouts.html#coloring
    -->
    <!-- <withJansi>true</withJansi> -->
    <encoder class="PatternLayoutEncoder">
      <charset>UTF-8</charset>
      <pattern>%d{yyyy-MM-dd HH:mm:ss} %-5level %cyan(%logger{36}) %magenta(%X{pekkoSource}) %msg%n</pattern>
    </encoder>
  </appender>

  <appender name="ASYNCFILE" class="AsyncAppender">
    <appender-ref ref="ROLLING_FILE"/>
  </appender>

  <appender name="ASYNCSTDOUT" class="AsyncAppender">
    <appender-ref ref="STDOUT"/>
  </appender>

  <logger name="play" level="INFO"/>
  <logger name="application" level="DEBUG"/>
  <logger name="utils" level="DEBUG"/>

  <root level="INFO">
    <appender-ref ref="ASYNCFILE"/>
    <appender-ref ref="ASYNCSTDOUT"/>
  </root>

</configuration>
